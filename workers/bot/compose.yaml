services:
  app:
    image: ghcr.io/tkgstrator/irodori-tts-discord-bot:latest
    env_file:
      - ../../.env
    environment:
      IRODORI_TTS_BASE_URL: http://irodori-tts:8088/v1
    depends_on:
      redis:
        condition: service_healthy
      irodori-tts:
        condition: service_healthy
    stdin_open: true
    tty: true

  dashboard:
    image: ghcr.io/tkgstrator/irodori-tts-dashboard:latest
    env_file:
      - ../../.env
    environment:
      REDIS_URL: redis://redis:6379
      IRODORI_TTS_BASE_URL: http://irodori-tts:8088/v1
      PORT: 18775
    ports:
      - 18775:18775
    depends_on:
      redis:
        condition: service_healthy

  # TTSサーバー本体は別リポジトリの成果物で、このリポのCIではビルドしない。
  # そのため Docker Hub のイメージを参照する。
  irodori-tts:
    image: tkgling/irodori-tts:latest
    volumes:
      - ./configs/config.yaml:/app/config.yaml:ro
      - ./models:/app/models:ro
      - hf_cache:/root/.cache/huggingface
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]
    healthcheck:
      test: [ "CMD", "curl", "-fsS", "http://localhost:8088/health" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 300s

  redis:
    image: redis:8.4.0
    volumes:
      - redis_data:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  redis_data:
    name: redis_data
  hf_cache:
    driver: local
